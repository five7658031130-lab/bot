# 🔐 Owner Commands Guide

**Bot**: @VexPulseBot  
**Owner**: @Sexuatic (ID: 7619316793)

## 🔑 Authentication

First, authenticate yourself as the owner:

```
/auth fiVe55@@
```

Once authenticated, you have access to all hidden owner commands.

---

## 📋 Basic Owner Commands

### 📊 Statistics & Monitoring
- `/stats` - View bot statistics (users, system resources)
- `/sysstats` - Detailed system statistics (CPU, RAM, Disk)
- `/dbstats` - Database statistics
- `/logs` - Get bot log files

### 📢 Broadcasting
- `/broadcast <message>` - Send message to all users
- `/broadcast` (reply to message) - Forward message to all users

### 🛠️ Maintenance
- `/cleanup` - Clean temporary files (downloads, cache, temp)
- `/cleardownloads` - Clear downloads folder only
- `/restart` - Restart the bot

---

## 🚀 Advanced Owner Commands

### 💻 System Control
- `/eval <python code>` - Execute Python code
  - Example: `/eval 2 + 2`
  - Example: `/eval len(authenticated_users)`
  
- `/shell <command>` - Execute shell commands
  - Example: `/shell ls -la`
  - Example: `/shell df -h`

### 👥 User Management
- `/banuser <user_id>` - Ban a user from using the bot
- `/unbanuser <user_id>` - Unban a user
- `/getuser <user_id or @username>` - Get detailed user information

### 💬 Direct Messaging
- `/sendmsg <user_id> <message>` - Send direct message to any user
  - Example: `/sendmsg 123456789 Hello! This is a test message`

---

## ⚡ Quick Command Reference

### For Daily Use:
```
/stats              - Check bot status
/logs               - Download logs
/cleanup            - Clean temp files
```

### For User Issues:
```
/getuser <id>       - Get user info
/banuser <id>       - Ban problematic user
/unbanuser <id>     - Unban user
/sendmsg <id> <msg> - Message specific user
```

### For Debugging:
```
/sysstats           - System resources
/dbstats            - Database info
/eval <code>        - Test Python code
/shell <cmd>        - Run system command
```

### For Announcements:
```
/broadcast <msg>    - Announce to all users
```

---

## 🔒 Security Notes

1. **Keep your password safe**: `fiVe55@@` 
2. Never share authenticated session
3. `/eval` and `/shell` are powerful - use carefully
4. Always test broadcast messages first
5. Authentication expires when bot restarts

---

## 💡 Pro Tips

1. **Test broadcasts**: Send to yourself first using `/sendmsg`
2. **Monitor resources**: Check `/sysstats` regularly
3. **Clean regularly**: Use `/cleanup` to free space
4. **Check logs**: Use `/logs` when debugging issues
5. **User management**: Get user info before banning

---

## 🆘 Emergency Commands

If bot is misbehaving:
1. `/logs` - Check what's wrong
2. `/sysstats` - Check resources
3. `/cleanup` - Free up space
4. `/restart` - Restart if needed

If users are spamming:
1. `/getuser <id>` - Verify user
2. `/banuser <id>` - Block them
3. `/broadcast` - Announce new rules

---

**Remember**: With great power comes great responsibility! 🕷️
